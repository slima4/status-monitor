{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "uptimepage-agent.serviceAccountName" . }}
  labels:
    {{- include "uptimepage-agent.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# The agent never calls the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
